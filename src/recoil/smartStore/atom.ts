import {atom} from "recoil";

export const reviewState = atom({
    key: "reviewState",
    default: [],
});

export const keywordState = atom({
    key: "keywordState",
    default: [],
});

export const productDateState = atom({
    key: "productDateState",
    default: [],
});